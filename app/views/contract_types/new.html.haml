%h1 Creating Contract Type

= error_messages_for :contract_type

%fieldset
  - smart_form_for(current_object) do |f|
    = render :partial => "form", :locals => {:f => f}
    %p= f.submit "Create"

= link_to 'Back', objects_path
