%h1 Creating Plot Attributes

= error_messages_for :plot_attributes

%fieldset
  - smart_remote_form_for(current_object) do |f|
    = render :partial => "form", :locals => {:f => f}
    %p= f.submit "Create"

= link_to 'Back', objects_path
