%h1 Creating <%= singular_name.titleize %>

= error_messages_for :<%= singular_name %>

%fieldset
  - smart_form_for(current_object) do |f|
    = render :partial => "form", :locals => {:f => f}
    %p= f.submit "Create"

= link_to 'Back', objects_path
